[pytest]
testpaths = tests
pythonpath = .
addopts = -q
markers =
    integration: tes HTTP via Flask test_client + DB (SQLite temp default, atau Postgres jika COFIND_INTEGRATION_BACKEND=postgres)
    api_contract: kontrak API (status, Content-Type, JSON Schema, auth & admin)
